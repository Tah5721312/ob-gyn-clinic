# 📋 توثيق الـ Frontend - نظام عيادة نساء وولادة

## 🎯 نظرة عامة

نظام بسيط وواقعي لعيادة دكتور نساء واحد مع موظف استقبال. التصميم بسيط وسهل الاستخدام لأن الدكتور مش فاضي ويكتب كتير.

---

## 📁 البنية العامة

### 1. **الصفحات الرئيسية** (`src/app/`)

#### **الصفحة الرئيسية** (`page.tsx`)

- **المسار**: `/`
- **الوصف**: لوحة التحكم الرئيسية - أصبحت الصفحة الرئيسية بدلاً من `/dashboard`
- **المميزات**:
  - عرض widgets مختلفة حسب الدور (DOCTOR, RECEPTIONIST, ADMIN)
  - إحصائيات حية من API
  - إجراءات سريعة
  - مواعيد اليوم
  - فواتير معلقة (للاستقبال)
  - ملخص مالي (للمدير)

#### **صفحة تسجيل الدخول** (`(auth)/signin/page.tsx`)

- **المسار**: `/signin`
- **الوصف**: صفحة تسجيل الدخول
- **المميزات**:
  - تصميم بسيط وجميل
  - توجيه تلقائي حسب الدور:
    - DOCTOR → `/`
    - RECEPTIONIST → `/appointments`
    - ADMIN → `/`
  - معالجة أخطاء واضحة

#### **صفحة المرضى** (`(protected)/patients/page.tsx`)

- **المسار**: `/patients`
- **الوصف**: قائمة المرضى مع بحث وفلترة
- **المميزات**:
  - بحث سريع بالاسم، الهاتف، أو الرقم القومي
  - فلتر بسيط (نشط/غير نشط)
  - جدول واضح يعرض: الاسم، الهاتف، العمر، المدينة، التأمين، الحمل، الحالة
  - زر "إضافة مريض جديد" يفتح Modal
  - إجراءات سريعة: عرض الملف، موعد جديد، زيارة جديدة

#### **صفحة المواعيد** (`(protected)/appointments/page.tsx`)

- **المسار**: `/appointments`
- **الوصف**: قائمة المواعيد
- **المميزات**:
  - عرض مواعيد اليوم افتراضياً (للطبيب: فقط مواعيده)
  - بحث بالاسم أو الهاتف (debounced بـ 300ms)
  - فلتر بالتاريخ والحالة
  - **تنقل بين الأيام**:
    - أزرار للانتقال لليوم السابق/التالي
    - اختيار التاريخ يدوياً من date picker
  - عرض حالة كل موعد (محجوز، مؤكد، مكتمل، ملغي، لم يحضر) مع أيقونات ملونة
  - عرض إذا كانت الزيارة تمت أم لا (علامة ✓ زيارة)
  - **تعديل موعد**: زر تعديل يفتح Modal مع بيانات الموعد الحالية
  - **حذف موعد**: زر حذف مع تأكيد قبل الحذف
  - زر "موعد جديد" يفتح Modal
  - جدول متجاوب مع hover effects
  - إعادة تحميل تلقائي بعد أي عملية (إضافة/تعديل/حذف)

#### **صفحة الفواتير** (`(protected)/billing/page.tsx`)

- **المسار**: `/billing`
- **الوصف**: قائمة الفواتير
- **المميزات**:
  - جدول بسيط يعرض: رقم الفاتورة، المريض، التاريخ، المبلغ الإجمالي، المدفوع، المتبقي، الحالة
  - بحث برقم الفاتورة أو اسم المريض
  - فلتر بالتاريخ والحالة
  - زر "فاتورة جديدة" يفتح Modal
  - تنسيق المبالغ بالعملة المصرية

#### **صفحة زيارة جديدة** (`(protected)/visits/new/page.tsx`)

- **المسار**: `/visits/new`
- **الوصف**: صفحة مبسطة لبدء زيارة جديدة للدكتور - **أهم صفحة في النظام**
- **المميزات**:
  - بحث سريع عن المريض
  - عرض مواعيد المريض المحجوزة (اختيار تلقائي إذا كان موعد واحد)
  - **Templates سريعة من قاعدة البيانات** (قوالب الروشتة فقط):
    - جلب قوالب الروشتة النشطة فقط (`templateType="روشتة"`)
    - عرض القوالب كأزرار مع أيقونة 💊
    - تطبيق القالب يملأ حقل الملاحظات تلقائياً:
      - إضافة التعليمات العامة (`generalInstructions`)
      - إضافة الأدوية مع تفاصيلها (الاسم، الجرعة، التكرار، المدة، التعليمات)
  - **زر "نفس الزيارة السابقة"**: يملأ الحقول من آخر زيارة للمريضة
  - **نموذج مبسط جداً**:
    - الشكوى الرئيسية (حقل نص واحد)
    - ملاحظات سريعة (textarea)
    - **تفاصيل إضافية (Collapsed)**:
      - العلامات الحيوية (الوزن، الضغط العلوي/السفلي، النبض)
      - نتائج الفحص
      - خطة العلاج
      - موعد المتابعة القادم
  - أزرار: حفظ + روشتة
  - حفظ الزيارة والانتقال لصفحة التفاصيل

#### **صفحة إدارة القوالب** (`(protected)/templates/page.tsx`)

- **المسار**: `/templates`
- **الوصف**: صفحة لإدارة القوالب (CRUD) - للدكتور فقط
- **المميزات**:
  - عرض جميع قوالب الدكتور
  - بحث بالاسم
  - فلتر حسب النوع (زيارة، روشتة)
  - إضافة قالب جديد
  - تعديل قالب موجود
  - حذف قالب
  - تفعيل/تعطيل القالب
  - إضافة/إزالة من المفضلة
  - دعم أنواع القوالب:
    - **زيارة**: تحتوي على `notes`, `chiefComplaint`, إلخ
    - **روشتة**: تحتوي على `medications`, `generalInstructions`
  - **عرض عدد الاستخدامات** (`usageCount`) لكل قالب
  - تحويل تلقائي للنص العادي إلى JSON إذا لم يكن JSON صالح
  - أمثلة واضحة لكل نوع قالب

#### **صفحة روشتة جديدة** (`(protected)/prescriptions/new/page.tsx`)

- **المسار**: `/prescriptions/new`
- **الوصف**: صفحة لإنشاء روشتة جديدة
- **المميزات**:
  - بحث سريع عن المريضة (debounced)
  - **دعم `patientId` من URL**: جلب بيانات المريض تلقائياً إذا كان موجود في URL
  - **دعم `visitId` من URL**: ربط الروشتة بزيارة موجودة
  - **أدوية شائعة**: أزرار سريعة (فيتامينات حمل، حديد، كالسيوم، مضاد حيوي)
    - تطبيق الدواء الشائع يملأ آخر دواء في القائمة تلقائياً
  - إضافة أدوية متعددة:
    - اسم الدواء (مطلوب)
    - الجرعة
    - التكرار
    - المدة
    - تعليمات إضافية
  - إضافة/حذف أدوية بسهولة
  - التحقق من وجود دواء واحد على الأقل قبل الحفظ
  - ملاحظات (اختياري)
  - حفظ الروشتة والانتقال لصفحة التفاصيل (`/prescriptions/[id]`)

---

## 🧩 المكونات (Components)

### 1. **Navigation** (`src/components/Navigation.tsx`)

- **الوصف**: شريط التنقل الرئيسي - **مبسط حسب الدور**
- **المميزات**:
  - شفاف وثابت في الأعلى
  - **روابط مختلفة حسب الدور**:
    - **DOCTOR**: الرئيسية | اليوم | خروج
      - ❌ لا يرى: المرضى، الفواتير
      - ✅ يرى فقط: الرئيسية، اليوم (المواعيد)
    - **RECEPTIONIST**: المرضى | المواعيد | الفواتير | خروج
    - **ADMIN**: المرضى | المواعيد | الفواتير | خروج
  - أزرار ملونة مع أيقونات
  - قائمة موبايل تفتح نفس العناصر
  - معلومات المستخدم وزر الخروج

### 2. **AuthButtons** (`src/app/components/header/AuthButtons.tsx`)

- **الوصف**: أزرار المصادقة
- **المميزات**:
  - عرض معلومات المستخدم (الاسم الكامل، الدور)
  - زر تسجيل الخروج
  - تصميم بسيط وواضح

### 3. **Dashboard Widgets**

#### **DoctorWidgets** (`src/components/dashboard/DoctorWidgets.tsx`)

- **الوصف**: Dashboard مبسط جداً للدكتور - **Action-based فقط**
- **المميزات**:
  - ❌ **لا توجد إحصائيات** (الدكتور مش محتاج أرقام)
  - ❌ **لا توجد قوائم** (تقيل على الدكتور)
  - ✅ **3 أزرار كبيرة فقط**:
    - **كشف جديد** → `/visits/new`
    - **مرضى اليوم** → `/appointments`
    - **روشتة سريعة** → `/prescriptions/new`
  - ✅ **زر إدارة القوالب** → `/templates` (أسفل الأزرار الكبيرة)
  - تصميم بسيط: أزرار كبيرة وواضحة
  - **الفلسفة**: الدكتور محتاج يبدأ كشف، مش محتاج يعرف أرقام

#### **ReceptionWidgets** (`src/components/dashboard/ReceptionWidgets.tsx`)

- **الإحصائيات**:
  - مواعيد اليوم
  - في الانتظار
  - مرضى جدد
  - فواتير معلقة
- **الإجراءات السريعة**:
  - حجز موعد جديد → Modal
  - تسجيل مريضة جديدة → Modal
  - بحث عن مريضة → `/patients`
  - إنشاء فاتورة → Modal
- **مواعيد اليوم**: عرض آخر 3 مواعيد مع أزرار إجراءات
- **فواتير معلقة**: عرض آخر فاتورتين معلقتين

#### **AdminWidgets** (`src/components/dashboard/AdminWidgets.tsx`)

- **الإحصائيات**:
  - إجمالي المرضى
  - مواعيد الشهر
  - إيرادات الشهر
  - فواتير معلقة
- **إدارة النظام**:
  - إدارة المستخدمين
  - التقارير المالية
  - إعدادات النظام
- **ملخص مالي**: إجمالي الإيرادات، المدفوع نقداً/بالكارت، المتبقي
- **إحصائيات سريعة**: عدد الزيارات، مرضى جدد، متابعات حمل، حالات سونار

### 4. **Shared Components** (`src/components/dashboard/shared/`)

#### **StatCard** (`StatCard.tsx`)

- بطاقة إحصائية بسيطة
- أيقونة ملونة
- قيمة كبيرة وواضحة
- عنوان صغير

#### **QuickActionButton** (`QuickActionButton.tsx`)

- زر إجراء سريع
- أيقونة emoji كبيرة
- عنوان ووصف
- تصميم gradient جميل

#### **AppointmentCard** (`AppointmentCard.tsx`)

- بطاقة موعد
- الوقت، اسم المريض، السبب
- حالة الموعد (في الانتظار، مكتمل، قادم)
- زر إجراءات (للاستقبال)

#### **InvoiceCard** (`InvoiceCard.tsx`)

- بطاقة فاتورة
- رقم الفاتورة، اسم المريض
- المبلغ الإجمالي، المدفوع، المتبقي
- حالة الدفع (مدفوع، جزئي، غير مدفوع)
- زر تسجيل دفعة

#### **FinancialRow** (`FinancialRow.tsx`)

- صف مالي بسيط
- التسمية والقيمة
- لون قابل للتخصيص

#### **TemplateButton** (`TemplateButton.tsx`)

- زر قالب بسيط
- تصميم border hover

#### **BigActionCard** (`BigActionCard.tsx`)

- بطاقة إجراء كبير - **للدكتور فقط**
- أيقونة emoji كبيرة (6xl)
- عنوان كبير وواضح
- وصف صغير (اختياري)
- ألوان مختلفة حسب الإجراء
- تأثير hover (scale + shadow)
- **الاستخدام**: Dashboard للدكتور - أزرار إجراءات رئيسية

---

## 🪟 Modals

### 1. **NewPatientModal** (`src/components/patients/NewPatientModal.tsx`)

- **الوصف**: Modal لإضافة مريض جديد
- **المميزات**:
  - خلفية شفافة مع blur (`bg-black/30 backdrop-blur-sm`)
  - نموذج بسيط:
    - البيانات الأساسية: الرقم القومي، الاسم، تاريخ الميلاد
    - فصيلة الدم والحالة الاجتماعية
    - معلومات الاتصال: الهاتف، العنوان
    - جهة الاتصال في الطوارئ
    - ملاحظات
  - إعادة تعيين تلقائي عند الإغلاق
  - إعادة تحميل قائمة المرضى بعد الحفظ

### 2. **NewAppointmentModal** (`src/components/appointments/NewAppointmentModal.tsx`)

- **الوصف**: Modal لإضافة أو تعديل موعد
- **المميزات**:
  - خلفية شفافة مع blur
  - دعم وضعين: إضافة جديد أو تعديل موعد موجود
  - بحث عن المريض
  - اختيار التاريخ والوقت
  - نوع الموعد (زيارة أولى، متابعة، سونار، استشارة، طوارئ)
  - المدة (15، 30، 45، 60 دقيقة)
  - ملاحظات
  - دعم `initialPatientId` من URL
  - عند التعديل: يتم ملء الحقول ببيانات الموعد الحالية
  - إعادة تحميل قائمة المواعيد بعد الحفظ

### 3. **NewInvoiceModal** (`src/components/billing/NewInvoiceModal.tsx`)

- **الوصف**: Modal لإضافة فاتورة جديدة
- **المميزات**:
  - خلفية شفافة مع blur
  - بحث عن المريض
  - تاريخ الفاتورة
  - المبالغ: المجموع الفرعي، الخصم، المبلغ الإجمالي (محسوب تلقائياً)، مبلغ التأمين
  - ملاحظات
  - إعادة تحميل قائمة الفواتير بعد الحفظ

### 4. **TemplateModal** (`src/components/templates/TemplateModal.tsx`)

- **الوصف**: Modal لإضافة أو تعديل قالب
- **المميزات**:
  - خلفية شفافة مع blur
  - نموذج شامل:
    - اسم القالب (مطلوب)
    - نوع القالب: زيارة أو روشتة
    - الفئة (اختياري)
    - المحتوى (JSON أو نص عادي)
    - تفعيل/تعطيل القالب
    - إضافة/إزالة من المفضلة
  - **تحويل تلقائي**: إذا كان المحتوى نص عادي وليس JSON، يتم تحويله تلقائياً:
    - للروشتة: `{"generalInstructions": "..."}`
    - للزيارة: `{"notes": "..."}`
  - أمثلة واضحة لكل نوع قالب
  - تحقق من صحة JSON مع رسائل خطأ واضحة
  - إعادة تعيين تلقائي عند الإغلاق

---

## 📋 قوائم البيانات (Lists)

### 1. **PatientList** (`src/components/patients/PatientList.tsx`)

- **المميزات**:
  - بحث سريع بالاسم، الهاتف، أو الرقم القومي
  - فلتر بسيط (نشط/غير نشط)
  - جدول واضح:
    - الاسم (قابل للنقر للانتقال للملف)
    - الهاتف (قابل للنقر للاتصال)
    - العمر
    - المدينة
    - التأمين (نعم/لا)
    - الحمل (حامل/غير حامل)
    - الحالة (نشط/غير نشط)
    - إجراءات (عرض الملف، موعد جديد، زيارة جديدة)
  - زر "إضافة مريض جديد" يفتح Modal
  - إعادة تحميل تلقائي بعد إضافة مريض

### 2. **AppointmentList** (`src/components/appointments/AppointmentList.tsx`)

- **المميزات**:
  - بحث بالاسم أو الهاتف (debounced)
  - فلتر بالتاريخ والحالة
  - **تنقل بين الأيام**: أزرار للانتقال لليوم السابق/التالي مع اختيار التاريخ يدوياً
  - عرض المواعيد في جدول:
    - اسم المريض
    - التاريخ والوقت (بتنسيق عربي)
    - اسم الدكتور
    - نوع الموعد (badge ملون)
    - حالة الموعد (محجوز، مؤكد، مكتمل، ملغي، لم يحضر) مع أيقونات
    - علامة إذا كانت الزيارة تمت (✓ زيارة)
    - **أزرار الإجراءات**: تعديل (✏️) وحذف (🗑️)
  - زر "موعد جديد" يفتح Modal
  - **تعديل موعد**: يفتح Modal مع بيانات الموعد الحالية
  - **حذف موعد**: مع تأكيد قبل الحذف
  - إعادة تحميل تلقائي بعد أي عملية

### 3. **InvoiceList** (`src/components/billing/InvoiceList.tsx`)

- **المميزات**:
  - بحث برقم الفاتورة أو اسم المريض
  - فلتر بالتاريخ والحالة
  - جدول بسيط:
    - رقم الفاتورة
    - اسم المريض
    - التاريخ
    - المبلغ الإجمالي
    - المدفوع
    - المتبقي
    - حالة الدفع (مدفوع، جزئي، غير مدفوع، ملغي)
  - زر "فاتورة جديدة" يفتح Modal
  - تنسيق المبالغ بالعملة المصرية
  - إعادة تحميل تلقائي بعد إضافة فاتورة

### 4. **TemplateList** (`src/components/templates/TemplateList.tsx`)

- **الوصف**: قائمة القوالب مع إدارة كاملة (CRUD)
- **المميزات**:
  - بحث بالاسم (debounced)
  - فلتر حسب النوع (زيارة، روشتة، الكل)
  - عرض القوالب في شبكة:
    - اسم القالب
    - النوع والفئة
    - **عدد الاستخدامات** (`usageCount`)
    - حالة النشاط (نشط/غير نشط) - badge ملون
    - حالة المفضلة (⭐) - زر قابل للنقر
  - أزرار الإجراءات:
    - ✏️ تعديل (يفتح Modal مع بيانات القالب)
    - 🗑️ حذف (مع تأكيد)
    - ⭐ إضافة/إزالة من المفضلة (تبديل فوري)
  - زر "إضافة قالب جديد" يفتح `TemplateModal`
  - تحديث تلقائي بعد أي عملية
  - رسالة عند عدم وجود قوالب مع زر لإنشاء قالب جديد

---

## 🎨 التصميم والأسلوب

### **الألوان والتصميم**:

- **الألوان الأساسية**:
  - الأزرق (`blue-600`) - للأزرار الرئيسية
  - الأخضر (`green-600`) - للنجاح/الإيجابي
  - الأحمر (`red-600`) - للأخطاء/الخروج
  - البرتقالي (`orange-600`) - للفواتير
  - البنفسجي (`purple-600`) - للمواعيد
  - الأزرق الداكن (`indigo-600`) - للمرضى

### **الأزرار**:

- تصميم موحد: `px-4 py-2 rounded-lg`
- ألوان مختلفة حسب الوظيفة
- أيقونات من `lucide-react`
- hover effects واضحة

### **Modals**:

- خلفية شفافة: `bg-black/30 backdrop-blur-sm`
- Modal في المنتصف: `fixed inset-0 z-50 flex items-center justify-center`
- إغلاق عند النقر على الخلفية
- زر X في الرأس
- تصميم بسيط وواضح

### **الجداول والقوائم**:

- تصميم بسيط وواضح
- hover effects
- ألوان للحالات (أخضر، أصفر، أحمر)
- أيقونات صغيرة للوضوح

---

## 🔐 المصادقة والصلاحيات

### **الأدوار**:

- **DOCTOR**: طبيب
  - **يرى**: الرئيسية، اليوم (المواعيد فقط)
  - **❌ لا يرى**: المرضى، الفواتير (المحاسبة = عبء نفسي)
  - **يمكنه**:
    - بدء زيارة جديدة (`/visits/new`)
    - عرض مواعيد اليوم (`/appointments`)
    - إنشاء روشتة (`/prescriptions/new`)
    - إدارة القوالب (`/templates`) - إنشاء وتعديل وحذف القوالب السريعة
  - **الفلسفة**: الدكتور محتاج 3 حاجات بس - يفتح كشف، يكتب كلمتين، يطلع روشتة
- **RECEPTIONIST**: استقبال
  - **يرى**: المرضى، المواعيد (كل المواعيد)، الفواتير
  - **يمكنه**: إضافة مواعيد، إضافة مرضى، إنشاء فواتير
- **ADMIN**: مدير
  - **يرى**: كل شيء
  - **يمكنه**: كل الإجراءات

### **التوجيه التلقائي**:

- عند تسجيل الدخول:
  - DOCTOR → `/`
  - RECEPTIONIST → `/appointments`
  - ADMIN → `/`
- عند محاولة الوصول لصفحة بدون تسجيل دخول → `/signin`

---

## 📱 التجاوب (Responsive)

- **Desktop**: عرض كامل مع جميع العناصر
- **Tablet**: تخطيط متجاوب
- **Mobile**:
  - قائمة موبايل منبثقة
  - جداول قابلة للتمرير
  - Modals متجاوبة

---

## ⚡ المميزات الرئيسية

### 1. **البساطة**:

- واجهات بسيطة وواضحة
- لا تعقيدات غير ضرورية
- سهلة الاستخدام للدكتور والاستقبال

### 2. **الواقعية**:

- تصميم واقعي كما في العيادات
- حقول ضرورية فقط
- سريع في الاستخدام

### 3. **Modals**:

- جميع عمليات الإضافة في Modals
- خلفية شفافة مع blur
- إغلاق سهل
- دعم التعديل (للمواعيد والقوالب)

### 4. **إحصائيات حية**:

- Dashboard يعرض إحصائيات حية من API
- تحديث تلقائي بعد العمليات

### 5. **البحث والفلترة**:

- بحث سريع في جميع القوائم (debounced بـ 300ms)
- فلترة بسيطة
- نتائج فورية
- بحث متقدم في المواعيد (بالاسم أو الهاتف)

### 6. **دعم URL Parameters**:

- **صفحة الزيارة الجديدة**: دعم `patientId` من URL لجلب المريض تلقائياً
- **صفحة الروشتة الجديدة**: دعم `patientId` و `visitId` من URL
- ربط تلقائي بين الصفحات عبر URL parameters

### 7. **التحديث التلقائي**:

- إعادة تحميل تلقائي للقوائم بعد أي عملية CRUD
- تحديث فوري بعد الحفظ/التعديل/الحذف
- لا حاجة لتحديث الصفحة يدوياً

### 8. **القوالب الذكية**:

- قوالب من قاعدة البيانات (ليست hardcoded)
- قوالب مفضلة تظهر أولاً
- قوالب نشطة فقط يتم عرضها
- تطبيق تلقائي للحقول عند استخدام القالب

---

## 🔄 التدفق (Flow)

### **إضافة مريض جديد**:

1. المستخدم يضغط "إضافة مريض جديد"
2. يفتح Modal
3. يملأ البيانات
4. يحفظ
5. Modal يغلق
6. قائمة المرضى تتحدث تلقائياً

### **إضافة موعد جديد**:

1. المستخدم يضغط "موعد جديد"
2. يفتح Modal
3. يبحث عن المريض
4. يختار المريض
5. يختار التاريخ والوقت
6. يحفظ
7. Modal يغلق
8. قائمة المواعيد تتحدث تلقائياً

### **إضافة فاتورة جديدة**:

1. المستخدم يضغط "فاتورة جديدة"
2. يفتح Modal
3. يبحث عن المريض
4. يملأ المبالغ
5. يحفظ
6. Modal يغلق
7. قائمة الفواتير تتحدث تلقائياً

### **بدء زيارة جديدة**:

1. الدكتور يضغط "كشف جديد" من Dashboard
2. يبحث عن المريضة (أو يتم جلبها تلقائياً إذا كان `patientId` في URL)
3. يتم جلب مواعيد المريضة المحجوزة تلقائياً
4. يختار موعد (أو يتم اختياره تلقائياً إذا كان موعد واحد فقط)
5. **يضغط Template سريع من قاعدة البيانات** (قوالب الروشتة النشطة فقط):
   - تطبيق القالب يملأ حقل الملاحظات تلقائياً
   - إضافة التعليمات العامة والأدوية مع تفاصيلها
6. أو يضغط "نفس الزيارة السابقة" لملء الحقول من آخر زيارة:
   - يتم جلب آخر زيارة للمريضة
   - يتم ملء جميع الحقول تلقائياً
   - يتم فتح تفاصيل إضافية تلقائياً إذا كانت هناك بيانات
7. يكتب الشكوى (سطر واحد)
8. يكتب ملاحظات سريعة
9. **يفتح تفاصيل إضافية** فقط إذا احتاج (العلامات الحيوية، إلخ)
10. يحفظ (ينتقل لصفحة تفاصيل الزيارة `/visits/[id]`)
11. زر "روشتة": حالياً يعرض رسالة (سيتم تطويره لاحقاً لفتح صفحة الروشتة مع ربطها بالزيارة)

### **إدارة القوالب**:

1. الدكتور يضغط "إدارة القوالب" من Dashboard
2. يرى قائمة بجميع قوالبه
3. يمكنه:
   - **إضافة قالب جديد**: يضغط "إضافة قالب" → يملأ البيانات → يحفظ
   - **تعديل قالب**: يضغط ✏️ → يعدل البيانات → يحفظ
   - **حذف قالب**: يضغط 🗑️ → يؤكد → يتم الحذف
   - **إضافة للمفضلة**: يضغط ⭐ → يظهر في صفحة الزيارة أولاً
   - **تفعيل/تعطيل**: من خلال التعديل
4. القوالب المفضلة تظهر أولاً في صفحة الزيارة
5. يمكن استخدام القوالب مباشرة من صفحة الزيارة

### **إنشاء روشتة جديدة**:

1. الدكتور يضغط "روشتة سريعة" من Dashboard
2. يبحث عن المريضة (أو يتم جلبها تلقائياً إذا كان `patientId` في URL)
3. **دعم `visitId` من URL**: إذا كان موجود، يتم ربط الروشتة بالزيارة تلقائياً
4. **يضغط دواء شائع** (فيتامينات حمل، حديد، كالسيوم، مضاد حيوي):
   - يملأ آخر دواء في القائمة تلقائياً
   - يمكن إضافة أدوية إضافية بعد ذلك
5. يضيف أدوية إضافية إذا احتاج (زر "إضافة دواء")
6. لكل دواء: اسم الدواء (مطلوب)، الجرعة، التكرار، المدة، تعليمات إضافية
7. يمكن حذف أي دواء (ما عدا الأخير إذا كان واحد فقط)
8. يكتب ملاحظات (اختياري)
9. التحقق من وجود دواء واحد على الأقل قبل الحفظ
10. يحفظ (ينتقل لصفحة تفاصيل الروشتة `/prescriptions/[id]`)

---

## 📦 الملفات الرئيسية

```
src/
├── app/
│   ├── page.tsx                          # الصفحة الرئيسية (Dashboard)
│   ├── layout.tsx                        # Layout مع SessionProvider
│   ├── (auth)/
│   │   └── signin/
│   │       └── page.tsx                  # صفحة تسجيل الدخول
│   └── (protected)/
│       ├── patients/
│       │   └── page.tsx                  # صفحة المرضى
│       ├── appointments/
│       │   └── page.tsx                  # صفحة المواعيد
│       ├── billing/
│       │   └── page.tsx                  # صفحة الفواتير
│       ├── visits/
│       │   └── new/
│       │       └── page.tsx              # صفحة زيارة جديدة (مبسطة)
│       ├── prescriptions/
│       │   └── new/
│       │       └── page.tsx              # صفحة روشتة جديدة
│       └── templates/
│           └── page.tsx                  # صفحة إدارة القوالب (CRUD)
├── components/
│   ├── Navigation.tsx                    # شريط التنقل
│   ├── dashboard/
│   │   ├── DoctorWidgets.tsx            # Dashboard للدكتور
│   │   ├── ReceptionWidgets.tsx         # Dashboard للاستقبال
│   │   ├── AdminWidgets.tsx             # Dashboard للمدير
│   │   └── shared/                      # مكونات مشتركة
│   │       ├── StatCard.tsx
│   │       ├── QuickActionButton.tsx
│   │       ├── AppointmentCard.tsx
│   │       ├── InvoiceCard.tsx
│   │       ├── FinancialRow.tsx
│   │       ├── TemplateButton.tsx
│   │       └── BigActionCard.tsx         # بطاقة إجراء كبير (للدكتور)
│   ├── patients/
│   │   ├── PatientList.tsx              # قائمة المرضى
│   │   └── NewPatientModal.tsx          # Modal إضافة مريض
│   ├── appointments/
│   │   ├── AppointmentList.tsx          # قائمة المواعيد
│   │   └── NewAppointmentModal.tsx      # Modal إضافة موعد
│   ├── billing/
│   │   ├── InvoiceList.tsx              # قائمة الفواتير
│   │   └── NewInvoiceModal.tsx         # Modal إضافة فاتورة
│   ├── templates/
│   │   ├── TemplateList.tsx            # قائمة القوالب مع CRUD
│   │   └── TemplateModal.tsx           # Modal إضافة/تعديل قالب
│   └── providers/
│       └── SessionProvider.tsx          # NextAuth SessionProvider
└── app/components/header/
    └── AuthButtons.tsx                   # أزرار المصادقة
```

---

## 🎯 المبادئ التصميمية

1. **البساطة أولاً**: واجهات بسيطة وواضحة
2. **الواقعية**: تصميم واقعي كما في العيادات
3. **السرعة**: سريع في الاستخدام
4. **التوافق**: يعمل على جميع الأجهزة
5. **الوضوح**: كل شيء واضح ومفهوم

## 🧠 فلسفة التصميم للدكتور

### **القاعدة الذهبية**:

> الدكتور عاوز 3 حاجات بس: يفتح كشف، يكتب كلمتين، يطلع روشتة

### **UX Policies للدكتور**:

1. **الدكتور لا يكتب أكتر من 3 حقول** في معظم الحالات
2. **أي Input ينفع يبقى Default** من آخر زيارة
3. **أي حاجة مش أساسية = Collapsed** (مثل العلامات الحيوية)
4. **أي Flow أكتر من 5 clicks = غلط**
5. **مفيش أرقام قدام الدكتور** (إحصائيات = عبء ذهني)
6. **المحاسبة = عبء نفسي** → الدكتور لا يرى الفواتير

### **Dashboard للدكتور**:

- ❌ **لا إحصائيات**
- ❌ **لا قوائم**
- ✅ **أزرار إجراءات كبيرة فقط** (Action-based)

---

## ✅ ما تم إنجازه

- ✅ صفحة تسجيل الدخول مع توجيه تلقائي
- ✅ صفحة Dashboard رئيسية - **Action-based للدكتور** (3 أزرار كبيرة فقط)
- ✅ صفحة المرضى مع بحث وفلترة
- ✅ صفحة المواعيد مع بحث وفلترة وتعديل وحذف
- ✅ صفحة المواعيد مع تنقل بين الأيام (prev/next)
- ✅ صفحة الفواتير مع بحث وفلترة (مخفية عن الدكتور)
- ✅ صفحة زيارة جديدة **مبسطة جداً** (شكوى + ملاحظات + تفاصيل collapsed)
- ✅ صفحة زيارة جديدة مع دعم `patientId` من URL
- ✅ صفحة زيارة جديدة مع قوالب الروشتة من قاعدة البيانات
- ✅ صفحة زيارة جديدة مع زر "نفس الزيارة السابقة"
- ✅ صفحة روشتة جديدة مع أدوية شائعة
- ✅ صفحة روشتة جديدة مع دعم `visitId` و `patientId` من URL
- ✅ Modal إضافة مريض جديد
- ✅ Modal إضافة/تعديل موعد جديد (يدعم التعديل)
- ✅ Modal إضافة فاتورة جديدة
- ✅ Navigation **مبسط حسب الدور** (الدكتور: الرئيسية | اليوم | خروج فقط)
- ✅ Dashboard widgets - **DoctorWidgets مبسط** (بدون إحصائيات)
- ✅ BigActionCard component (أزرار إجراءات كبيرة)
- ✅ Templates سريعة في صفحة الزيارة (من قاعدة البيانات)
- ✅ صفحة إدارة القوالب (`/templates`) مع CRUD كامل
- ✅ TemplateList و TemplateModal components
- ✅ دعم القوالب المفضلة والنشطة
- ✅ تحويل تلقائي للنص العادي إلى JSON في القوالب
- ✅ زر "إدارة القوالب" في DoctorWidgets
- ✅ تطبيق قوالب الروشتة في صفحة الزيارة
- ✅ عرض عدد الاستخدامات (`usageCount`) في قائمة القوالب
- ✅ تصميم موحد وبسيط
- ✅ Modals مع خلفية شفافة
- ✅ بحث debounced في جميع القوائم
- ✅ إعادة تحميل تلقائي بعد أي عملية CRUD

---

## 🔌 API Routes

### **Templates API** (`src/app/api/templates/`)

#### **GET `/api/templates`**

- جلب جميع القوالب للدكتور
- Query params: `doctorId`, `search`, `templateType`, `isActive`
- إرجاع: قائمة القوالب

#### **POST `/api/templates`**

- إنشاء قالب جديد
- Body: `templateName`, `templateType`, `category`, `content` (free text), `isActive`, `isFavorite`, `doctorId`
- إرجاع: القالب المُنشأ

#### **GET `/api/templates/[id]`**

- جلب قالب واحد بالمعرف
- إرجاع: بيانات القالب

#### **PUT `/api/templates/[id]`**

- تحديث قالب موجود
- Body: نفس بيانات POST
- إرجاع: القالب المحدث

#### **DELETE `/api/templates/[id]`**

- حذف قالب
- إرجاع: رسالة نجاح

#### **GET `/api/templates/favorites`**

- جلب القوالب المفضلة للدكتور
- Query params: `doctorId`
- إرجاع: قائمة القوالب المفضلة

#### **POST `/api/templates/favorites`**

- تبديل حالة المفضلة للقالب
- Body: `templateId`
- إرجاع: القالب المحدث

**ملاحظة**: جميع routes تستخدم `params` كـ Promise في Next.js 13+ App Router.

---

## 🚀 الخطوات التالية (اختياري)

- [ ] صفحة تفاصيل المريض
- [ ] صفحة تفاصيل الموعد
- [ ] صفحة تفاصيل الفاتورة
- [ ] صفحة تفاصيل الزيارة
- [ ] صفحة تفاصيل الروشتة
- [ ] صفحة تعديل البيانات
- [ ] تقارير وإحصائيات متقدمة (للاستقبال والمدير فقط)
- [x] Templates متقدمة (حفظ واستخدام) ✅ **تم إنجازه**
